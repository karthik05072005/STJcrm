import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev indicator badge (the floating "N" logo) during development.
  devIndicators: false,
};

export default nextConfig;
